import React from "react";
import { SimpleTabScreen } from "@/components/shell/simple-tab-screen";

export default function CalendarScreen() {
  return (
    <SimpleTabScreen
      title="Calendar"
      subtitle="See your cooking plans at a glance."
      body="Weekly meal planning and scheduling lives in this destination."
    />
  );
}
